name: Daily News Email

on:
  schedule:
    # 每天北京时间 19:00 (UTC 11:00) 运行
    - cron: '0 11 * * *' 
  workflow_dispatch:

# 核心修复：强制所有的 JavaScript Action 在 Node.js 24 下运行，消除弃用警告
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  issues: write
  contents: read

jobs:
  send-newsletter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # 虽然内部声明 Node 20，但会被 env 强制提升

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser resend akshare pandas requests

      - name: Run Portfolio Analysis
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          python main.py

      - name: Send notification on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '❌ 投研日报任务运行失败',
              body: '脚本运行出现错误。请检查 Actions 运行日志。'
            })
